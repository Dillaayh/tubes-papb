name: wuunderlist
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # UI & Utilities
  cupertino_icons: ^1.0.8
  go_router: ^14.6.2
  provider: ^6.1.2
  google_fonts: ^6.2.1
  flutter_staggered_animations: ^1.1.1
  intl: ^0.20.2
  shared_preferences: ^2.2.3

  # ✅✅✅ TAMBAHKAN DUA BARIS INI ✅✅✅
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.4

  # Database - Drift
  drift: ^2.14.1
  sqlite3_flutter_libs: ^0.5.18
  path_provider: ^2.1.1
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # Code Generation untuk Drift
  drift_dev: ^2.14.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
