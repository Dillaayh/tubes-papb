// Model Category (Opsional - tidak dipakai dalam app saat ini)
// Hanya untuk reference jika nanti mau custom category

class Category {
  final String id;
  final String name;

  const Category({
    required this.id,
    required this.name,
  });
}